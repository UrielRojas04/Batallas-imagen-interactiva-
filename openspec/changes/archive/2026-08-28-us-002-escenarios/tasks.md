## 1. Assets

- [x] 1.1 Mover la carpeta `img/` a `public/img/`.

## 2. Creación de Componentes

- [x] 2.1 Crear `src/components/MainCharacter.jsx` para renderizar `principal.jpg` de manera fija.
- [x] 2.2 Crear `src/components/NormalScenario.jsx` que incluya el fondo `escenario normal.jpg` y las nubes `nubes.jpg`.
- [x] 2.3 Crear `src/components/SpiritualScenario.jsx` que incluya el fondo espiritual y los objetos (ángeles y demonios).

## 3. Integración en Layout

- [x] 3.1 Agregar CSS en `index.css` o archivos propios para posicionar correctamente los componentes (layout horizontal) y aplicar las transformaciones parallax básicas.
- [x] 3.2 Modificar `App.jsx` para reemplazar el div dummy por los componentes reales (`MainCharacter`, `NormalScenario`, `SpiritualScenario`).
