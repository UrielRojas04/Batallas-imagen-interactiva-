## 1. Componente Genérico Interactivo

- [x] 1.1 Crear `src/components/InteractiveObject.jsx` que encapsule una imagen, aplique la clase `parallax-layer`, acepte el `parallaxFactor`, y maneje el estado de click/touch (`isInteracting`) para añadir una clase CSS temporal de animación.

## 2. Refactorización de Escenarios

- [x] 2.1 Refactorizar `SpiritualScenario.jsx` para que use `InteractiveObject` en lugar de divs planos para los ángeles y demonios.

## 3. Animaciones CSS

- [x] 3.1 Agregar keyframes `@keyframes interact-pop` en `App.css` y la clase `.interacting` que aplique la animación a las imágenes.
